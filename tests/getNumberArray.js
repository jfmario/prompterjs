
var prompter = require ( '../index' );

console.log ( prompter.getNumberArray ( "What are your children's ages?" ) );
console.log ( prompter.getIntArray ( "What are your children's ages (ints)?" ) );
